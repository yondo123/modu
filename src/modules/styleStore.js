const state = {
    mobile: window.innerWidth < 1024 ? true : false
};
const getters = {};
const mutations = {};
const actions = {};

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions
};
